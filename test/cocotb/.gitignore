# Simulation build artifacts for the cocotb suites - all regenerated by
# `make sim-vcs` in each suite (filelists are emitted by the `filelist` target)
sim_build/
__pycache__/
*.f
results.xml
ucli.key
vc_hdrs.h
sim_vcs.log

# Wave dumps and tool session logs
*.vcd
*.vpd
*.fst
*.fsdb
verdiLog/
vcd2fsdbLog/
novas.*
signal.rc
